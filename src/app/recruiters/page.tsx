import { RecruiterLandingPage } from "@/components/landing/RecruiterLandingPage";

export const metadata = {
  title: "Vodora for Recruiters — Hire Faster, Verify Once",
  description:"Access pre-verified candidates with authenticated references. Cut time-to-hire and skip manual reference checks.",
};

export default function RecruitersPage() {
  return <RecruiterLandingPage />;
}